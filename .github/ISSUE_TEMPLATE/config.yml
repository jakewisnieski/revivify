# Keep the "open a blank issue" escape hatch — a solo builder shouldn't be
# forced through a template for a quick note. The Feature/Bug templates are
# the encouraged default; see docs/github-workflow.md.
blank_issues_enabled: true
